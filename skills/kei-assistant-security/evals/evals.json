{
  "skill_name": "kei-assistant-security",
  "evals": [
    {
      "id": 1,
      "prompt": "I need to add a new environment feature flag that enables an optional diagnostic endpoint. Apply the assistant's master-switch invariants so the flag is fail-closed and dark by non-construction when not enabled.",
      "expected_output": "Uses strict-equality comparison to the literal \"true\", builds the endpoint objects only when enabled, and refuses non-canonical truthy values.",
      "expectations": [
        "The response does not treat \"TRUE\", \"1\", or \" true \" as enabled.",
        "The response constructs the endpoint components only inside the enabled branch, not before a runtime check."
      ]
    },
    {
      "id": 2,
      "prompt": "A penetration test report says the assistant returns a different HTTP body for an unknown bot token vs a known expired token. Should I enrich the error body to help the operator debug?",
      "expected_output": "Rejects enrichment and explains that deny responses must remain indistinguishable to avoid leaking gate internals, citing the pseudonymous audit and closed audit-code invariants.",
      "expectations": [
        "The response does not recommend adding detail to distinguish denial reasons.",
        "The response cites the pseudonymous audit and closed-code invariants from the assistant's security design."
      ]
    }
  ]
}
